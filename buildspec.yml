version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install
      - npm -v
  build:
    commands:
      - echo Build step 
      
  post_build:
    commands:
      - echo Build completed on `date`

artifacts:
  files:
    - '**/*'
    - '!node_modules/**/*'   # Exclude node_modules if you want a smaller artifact